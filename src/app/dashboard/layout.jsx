export const metadata = {
  title: "Dashboard",
  description:
    "Welcome to the Eduelle dashboard. Manage your courses and settings.",
};

export default function DashboardLayout({ children }) {
  return <div>{children}</div>;
}
